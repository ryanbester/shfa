task buildAll(type: GradleBuild) {
    group = 'shfa'
    tasks = [
            'forge:build',
            'fabric:build'
    ]
}

task forgeClient(type: GradleBuild) {
    group = 'shfa'
    tasks = [
            ':forge:runClient'
    ]
}

task fabricClient(type: GradleBuild) {
    group = 'shfa'
    tasks = [
            ':fabric:runClient'
    ]
}
